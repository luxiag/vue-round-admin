export enum ThemeEnum {
  dark = 'dark',
  light = 'light',
  // 色弱模式
  weak = 'weak',
}

// 布局模式
export enum LayoutEnum {
  default = 'leftTopMode',
  leftTop = 'leftTopMode',
  topLeft = 'topLeftMode',
  top = 'topMode',
  left = 'leftMode',
}
